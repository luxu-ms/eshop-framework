<?xml version="1.0"?>
<configuration>
  <system.web>
    <authorization>
      <allow users="?" />
      <!-- Uncomment the line below to restrict admin to logged-in users only -->
      <!-- <deny users="?" /> -->
    </authorization>
  </system.web>
</configuration>
